# Switch the low-level controller mode (e.g., computed torque vs PID).
#
# Request --------------------------------------------------------------------
# Desired mode keyword (e.g., "ct" for computed torque, "pid" for PID).
string mode
---
# Response -------------------------------------------------------------------
# True if the controller accepted the new mode.
bool success

# Human-readable status or error description.
string message
