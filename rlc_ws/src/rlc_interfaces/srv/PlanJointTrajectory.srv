# Request a time-parameterized joint trajectory to reach target positions.
#
# Request --------------------------------------------------------------------
# Target joint positions (rad) ordered according to the controller joint map.
float64[] target_positions

# Desired time to reach the target from the start of the plan.
builtin_interfaces/Duration time_from_start

# frequency of the planned trajectory (Hz).
float64 frequency

# If true, trigger execution immediately after planning.
bool execute_immediately
---
# Response -------------------------------------------------------------------
# True when a trajectory was successfully generated (and optionally queued).
bool success

# Identifier for the planned trajectory (valid when success is true).
string trajectory_id

# Human-readable status or error description.
string message
